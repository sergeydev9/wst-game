import Button from "./BlueBorderButton";

export default {
    component: Button,
    title: "Blue Border Button"
}

const Template = () => {
    return (
        <Button>Contact Us</Button>
    )
}

export const BlueButton = Template.bind({})